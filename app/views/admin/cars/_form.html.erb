<%= form_for(@admin_car) do |f| %>
  <% if @admin_car.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_car.errors.count, "error") %> prohibited this admin_car from being saved:</h2>

      <ul>
      <% @admin_car.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
