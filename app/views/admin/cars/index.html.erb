<h1>Listing admin_cars</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admin_cars.each do |admin_car| %>
  <tr>
    <td><%= admin_car.name %></td>
    <td><%= admin_car.description %></td>
    <td><%= link_to 'Show', admin_car %></td>
    <td><%= link_to 'Edit', edit_admin_car_path(admin_car) %></td>
    <td><%= link_to 'Destroy', admin_car, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Car', new_admin_car_path %>
